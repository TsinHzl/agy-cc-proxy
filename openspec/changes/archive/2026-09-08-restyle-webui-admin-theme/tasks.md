# 变更提案：restyle-webui-admin-theme

## 状态：ARCHIVED

## 任务

- [x] 1. 设计令牌与字体基座：全量重写 `public/css/src/input.css`（移植 kiro2cc `:root`/`.dark` 全部 CSS 变量、IBM Plex @font-face（或独立 fonts.css）、6px 滚动条、`.bg-grid-dot`、重定义 .view-card/.view-header/.standard-table/.custom-range/.btn-action-*/.status-pill*/.input-search/.filter-control/.nav-item 等语义类为新主题等价物）；**旧主题变量保名换值**（--color-neon-purple/green/cyan、--color-space-950/900/800/border、--color-text-main/bright/dim/muted、--color-chart-1..16 保留原名指向新令牌色，供 charts.js 15 处 + app.js 2 处 getThemeColor 消费，防止图表回退霓虹 fallback；--color-space-950 作 tooltip 背景/边框色单列，映射为浅色主题下的深色 tooltip 语义值）；更新 `tailwind.config.js`（字体栈 IBM Plex Sans/Mono、DaisyUI 主题改 kiro2cc 浅色配色）；从 kiro2cc admin-ui/public/fonts/ 复制 8 个 woff2 至 `public/fonts/` 并新建 `public/fonts.css`（14 个 @font-face）。验证：`npm run build:css` 构建成功且产物非空（需重启无要求，前端静态文件）
- [x] 2. 布局骨架：重写 `public/index.html` —— 去 navbar、固定左侧栏 232px + bg-grid-dot 点阵纹理、主内容 px-9 py-7、侧栏 active 项（bg-brand-soft + 2.5px brand 竖条）、modal 与 Toast 容器换肤；**移除 html 标签写死的 `class="dark"` 与 `data-theme="antigravity"`**；保留全部 Alpine 绑定、x-load-view 容器、script 加载顺序、fonts.css `<link>` 引入。验证：页面加载后侧栏/视图容器/6 个 x-load-view 均正常渲染且为浅色主题（浏览器人工核验）
- [x] 3. 登录页：`public/login.html` 复刻 kiro2cc login-page 样式（浅色 surface 卡片、IBM Plex 字体、新按钮/输入框规格）；**同步移除 html 标签的 `class="dark"`/`data-theme`**。验证：登录流程（原生 JS handleLogin）功能不变，redirect 校验逻辑不动，页面默认浅色。（执行注记：`<html>` 标签的 `data-theme="antigravity"`/`class="dark"` 已移除；build:css 后 23 个新工具类全部收录进产物；handleLogin/redirect 校验逐字未动）
- [x] 4. Dashboard 页：`public/views/dashboard.html` 换肤 —— 指标区复刻 metrics 指标条规格（rounded-[16px] 外壳 + 1px 分隔线 + 28px/700 主值 + 状态点），四档渐变进度条（剩余语义右对齐 + h-1 + rounded-[3px]）+ QuotaPercentBadge 风格胶囊；Chart.js 容器与图例颜色适配新令牌。验证：数据渲染与图表出图正常（浏览器人工核验）。（执行注记：页头 PageHead 化 + Live 状态点改 side-foot 规格；指标区改 metrics-bar 通栏 5 段（input.css 新增 .metrics-bar/.metric-cell/.metric-divider 响应式类，<lg 降级 2 列）；tier 徽章改 status-pill-ultra/pro/free；quotaChart 环形图改 MetricAside 悬浮 + 图例点 bg-brand/bg-ok；警告横幅改 warn-soft/warn-line 语义；下拉面板改 bg-surface + border-hairline + shadow-pop；charts.js 零改动经保名换值自动映射；全部 Alpine 绑定逐字保留；build:css 后新工具类（.metrics-bar/.metric-cell/.metric-divider/.ring-[3px]/.ring-ok-soft 等）全部收录进产物。CR 修复注记：M1 分隔线改 mobile-first（默认 display:none，≥lg 恢复 block，消除 1023.5px 分数视口盲区）+ M2 补 button.metric-cell:focus-visible 键盘焦点态，产物已重新核验收录）
- [x] 5. Accounts 页：`public/views/accounts.html` 换肤 —— 表格复刻 table-kit 规格（PANEL rounded-[11px] + TH 大写小字 + CELL 边框行）、账号列头像+昵称+脱敏邮箱结构、额度列四档渐变进度条 + 百分比胶囊、行内开关复刻 Switch 规格（h-[18px] w-8 rounded-[10px] data-checked:bg-brand）；保留 FIX/APPEAL/设置/刷新/删除操作与 3 个 modal 功能。验证：账号列表渲染、quota modal、删除确认流程正常。（执行注记：页头 PageHead 化；表格 PANEL `max-h-[70vh]` sticky TH + `border-separate`；开关用 `has-[:peer-checked]:bg-brand` 变体实现（无 Radix 的 Alpine 等价物）；账号列 26px 状态色头像 + formatEmail 双行；额度列四档 >60/>30/>15 渐变条 + quota-badge 胶囊；Health .tag 徽标 + 5px pip；操作列 ICON_BTN + 分隔线；Strategy Health Inspector / delete / quota / threshold 三 modal 全部换肤（bg-surface + border-hairline + shadow-pop + 语义色），threshold modal 的 custom-range-yellow 移除（基础 .custom-range 默认 brand 色即新主题等价物）；空状态 colspan 修正 6→7；全部 Alpine 绑定逐字保留；build:css 后新工具类（size-[26px]/rounded-[11px]/has-[:peer-checked]/translate-x-[14px]/ring-[2.5px]/shadow-panel/quota-badge-*/bg-danger/bg-warn 等）全部收录进产物）
- [x] 6. Models 页：`public/views/models.html` 换肤 —— 复刻 model-list-page 分组行规格（bg-surface-2 + 大写小字 + mono 数量）、倍率条（h-1 rounded-[3px] bg-track + bg-brand/最高档 bg-warn）、额度可拖拽 threshold 标记保留、Account Distribution 方块适配新令牌。验证：排序/过滤/展开 per-account breakdown/pin+hide 功能正常。（执行注记：整文件重写 —— ①PageHead 化（modelsPageDesc 面包屑 + 19px h1 + SearchBox input-search-sm ml-auto + 清除钮）②操作条 Toolbar 化（账号 select h-[31px] rounded-[7px] + family Segmented（bg-surface-3 容器 + 激活 bg-surface shadow-hair font-semibold）+ Show Hidden/Show All Accounts 切换钮（激活 brand-soft/brand-line、未激活 hairline/surface-3 hover））③表格 PANEL 化（rounded-[11px] border-hairline bg-surface shadow-panel + TH_BASE 6 列 sticky 排序（compact 4 列）+ 数据行 `[&>td]:border-b [&>td]:border-hairline` + hover:bg-surface-2）④分组行按 family 引入（经用户确认：模板层嵌套 x-for over claude/gemini/other + tbody.contents 包裹、pinned 行保持排序首位不分组；分组行 bg-surface-2 + 10.5px 大写 tracking-0.07em + 13px 盒子图标 + mono 行数 + 右侧该组可用账号去重计数）⑤额度条 `<progress>` 改 .quota-bar + quota-tier-safe/mod/warn/danger 四档渐变（>60/>30/>15 分档，与 accounts.html 先例一致）⑥threshold 拖拽标记结构逐字保留（group/marker + scale-y-150 + shadow-pop tooltip，tooltip 换 bg-space-950 border-hairline）⑦Distribution 方块 h-2.5 w-2.5 rounded-[2px] bg-ok/warn/danger + 溢出 +N 徽标（surface-3 + border-hairline-2）⑧pin/hide 改 ICON_BTN 规格（size-[26px] rounded-[6px] + 激活 brand-soft/danger-soft）+ Loading/空态改 size-6 图标 + text-[12.5px] 规格；全部 Alpine 绑定（x-model/x-for/setSort/startDrag/updateModelConfig/toggleExpanded/getVisibleAccounts）逐字保留，data-store.js 与 models.js 零改动；build:css 成功，新工具类（rounded-[11px]/size-[26px]/text-[12.5px]/py-[11px]/py-[9px]/py-[7px]/tracking-[0.07em]/text-[10.5px]/h-[13px]/group\/marker/[&>td]:border-b/[&:last-child>td]:border-b-0 等）全部核验收录进产物）
- [x] 7. Usage-Log 页：`public/views/usage-log.html` 换肤 —— 终端工具栏改为 kiro2cc toolbar 规格（h-[31px] 搜索框/按钮、UpdatedAgo ml-auto 小字）、6 列 sticky 表格复刻 TH/CELL 规格。验证：copyAll/refreshData 功能正常
  - 执行注记：①PageHead（面包屑 + 19px h1 + 236px 客户端过滤搜索框 x-model="search"）②PANEL（rounded-[11px] shadow-panel + max-h-[70vh] overflow-y-auto + 6 列 TH_BASE sticky + CELL 数据行 hover:bg-surface-2）③PANEL_FOOT（totalRecords + Total Credits + ICON_BTN copy/refresh + UpdatedAgo ml-auto 30s agoTick 心跳）④三态空态拆分（loading / 搜索无命中 noMatchingModels / 无记录 noUsageRecords，kiro2cc size-6 图标规格）⑤usage-log-viewer.js 新增 search/lastUpdated/agoTick/filteredRecords/filteredCredits/formatAgo 五档相对时间/copyAll 改 filteredRecords/destroy() 定时器清理；en/zh 补 6 个 i18n key（updatedAgo/justNow/secondsAgo/minutesAgo/hoursAgo/daysAgo，单大括号插值）；npm run build:css 产物核验通过（全部任意值类编译成功）；CR 2 轮：第 1 轮 FAIL（1 HIGH agoTick 失效 + 4 MED）→ 修复后第 2 轮 PASS（残留 1 MED :key 碰撞面记入 cr-known-issues，用户确认不修）
- [x] 8. Logs 页：`public/views/logs.html` 换肤 —— 复刻 log-viewer-page 规格（.logstream/.logrow/.lv 徽章五档 LEVEL_PIP：ERROR bg-danger/WARN bg-warn/INFO bg-brand/DEBUG bg-ink-3/TRACE bg-hairline-2、×N 折叠徽标、空态规格）；级别 checkbox 与 Auto-Scroll/导出/清空控件换肤。验证：日志流渲染、Redact 脱敏、级别过滤功能正常
  - 执行注记：①根容器改 view-container-full h-full（index.html 挂载容器配合全高内部滚动）②PageHead 化（面包屑 + 19px h1 + 236px SearchBox input-search-sm + 清除钮）③PANEL 壳 logpanel（rounded-[11px] + min-h-0 flex-1 内部滚动）+ PANEL_TITLE ④Toolbar 化：级别 Segmented 五段（ALL + INFO/SUCCESS/WARN/ERROR，pip：INFO bg-brand/SUCCESS bg-ok/WARN bg-warn/ERROR bg-danger，ALL 段带 logs.length count）替代 4 个 checkbox；Auto-Scroll 改 kiro2cc Toggle 规格（34×19 role=switch 轨道 + 14px 白点）；导出/清空改 ICON_BTN（size-[26px]）；移除终端三圆装饰与闪烁光标 ⑤日志区 .logstream + .logrow（ts + lv + msg，ERROR 行 is-err）；.lv 静态档类防 purge（SUCCESS 无 .lv.success 档，模板层内联 text-ok bg-ok-soft border-ok-line）；Redact.logMessage().replace(/\n/g,'<br>') 逐字保留 ⑥空态两态拆分（emptyStreamHint / emptyNoMatch）⑦logs-viewer.js 新增 setLevel()/isLevelAll() 两个模板层映射方法（Segmented 单选 vs filters 布尔多选桥接；DEBUG 仍由 settings.debugLogging 联动、不进 Segmented），SSE/rAF/scrollToBottom/clearLogs/exportLogs 核心逻辑零改动 ⑧en/zh 补 2 个 i18n key（emptyStreamHint/emptyNoMatch，单大括号插值）⑨build:css 成功，全部新工具类核验收录进产物（×N 折叠徽标与 MetricsBar 经用户确认不做）
- [x] 9. Settings 页：`public/views/settings.html`（1989 行，按 Tab 分步）换肤 —— 复刻 settings-panel 规格：SET_CAP 分区标题 / SET_CARD 分区卡 / SET_ROW 设置行 / FIELD_S 只读值框 / SEG+SEG_BTN 分段控件；Models Tab（模型映射表/pin/hide）、Server Tab（Server Presets、Developer Mode 嵌套开关、Advanced Tuning 折叠滑条组、Password Dialog）全部控件换为新主题。验证：各 Tab 切换、config 编辑（UI/JSON 双模式）、滑条调节、开关 toggle、password dialog 功能正常
  - 执行注记：①Models Tab：模型表 PANEL 化（view-card !p-0 border-hairline）+ 行 hover:bg-surface-2 + group-hover:text-ink；pin/hide 圆钮换语义色（pin 激活 brand-soft/brand-line/text-brand，hide 激活 danger-soft/danger-line/text-danger，未激活 btn-ghost text-ink-3）；空态 text-ink-3；DEFAULT MODEL ALIASES divider 换 text-ink-3 ②Server Tab：Server Presets 卡按钮区（load/edit/delete 方形图标按钮规格 + danger 变体 + 内联 SVG spinner 替代 DaisyUI loading）、preset 描述盒（rounded-[7px] border-hairline bg-surface-2）、preview 编辑区（UI/JSON 分段 bg-brand 激活 + strategy badge brand 语义 + textarea/input/错误态 danger-line + Save/Cancel 小文字按钮 h-[26px] + 内联 spinner）、Save Server Preset Modal（bg-surface + border-brand-line + shadow-pop + h3 text-ink + p text-ink-3 + h-[31px] 输入/按钮 + 内联 spinner）③DevMode 卡 SET_CARD + hover/激活 border-brand-line + 标题激活 text-brand + 主/子 toggle 全部换 role=switch SW 规格（h-[18px] w-8 rounded-[10px] bg-brand/bg-track + size-3 白点 + aria-checked .toString()；placeholderMode 子项连带修复孤儿 </label> 残留 + Include Real Accounts SW 化）④Token Cache 卡 hover/激活 ok 语义 + bg-ok 激活 SW；Max Accounts 卡 SET_CARD + FIELD_S 数字输入；Strategy 分隔 SET_CAP + select FIELD_S 全宽变体 ⑤Advanced accordion：容器 view-card border-hairline + 头部图标盒（border-hairline-2 bg-surface-2 text-brand）/标题 text-[12.5px] text-ink/chevron text-ink-3 + hover:bg-surface-2；展开区分隔 bg-hairline；Network Retry / Rate Limiting / Quota Protection / Error Handling 四分区标题全换 SET_CAP（text-[10.5px] uppercase tracking-[.08em] text-ink-3）+ 分隔 border-t border-hairline；全部滑条 label-text 换 text-ink-3、label-text-alt 数值换 brand（green 档 text-ok）/mono 语义色；number input 全换 FIELD_S 规格（h-[31px] rounded-[7px] border-hairline-2 bg-surface-2 text-center font-mono text-[12px] text-ink-2 focus:border-brand）；说明文字全换 text-ink-3 ⑥Request Throttling：Experimental 徽标 warn 语义（border-warn-line bg-warn-soft text-warn）+ 卡激活 border-warn-line bg-warn-soft（去 glow/ring）+ 标题激活 text-warn + toggle 换 SW（激活 bg-ok）+ 分隔 border-hairline + delay input border-warn-line ⑦Password Dialog：容器 bg-surface + border-brand-line + shadow-pop + h3 text-ink + label text-ink-2 + 3 个 input 换 FIELD_S 全宽 + Cancel 中性 / Save bg-brand h-[31px] 按钮 ⑧Info Notice footer text-ink-3 italic；全部 Alpine 绑定/x-model/@click 逐字保留；grep 复核 space-800/space-900/space-border/gray-300-600/neon-*/amber-*/red-500 零残留
- [x] 10. 图表与全局收尾：`public/app.js` Chart.defaults 更新（**font.family 由 JetBrains Mono 改 IBM Plex Mono（app.js:101）**、颜色变量换新令牌）+ **移除 `data-theme='black'`/`classList.add('dark')` 强制暗色设置（app.js:94-95）**；核对 charts.js 15 处 + app.js 2 处 getThemeColor 旧变量经保名换值后图表颜色正确、霓虹 fallback（`#a855f7` 等）未被触发；**处理 js/components 内硬编码霓虹色**：`public/js/components/models.js:11-20` thresholdColors 8 组硬编码 hex 与 `public/js/components/model-dropdown.js:72-73` `focus:!border-neon-purple`/`focus:!border-neon-cyan` 类名，改用 CSS 变量或新令牌色；检查其余 `js/components/*.js`（仅 models.js body.select-none 与 usage-log-viewer.js 剪贴板 textarea 不受影响）；全站灰度细节核对（滚动条、focus ring、x-transition）。验证：`npm run build:css` 最终构建成功；浏览器遍历 8 个页面（index + login + 6 views）无样式断裂、控制台无报错、图表无霓虹色残留
  - 执行注记：①app.js 移除 `data-theme='black'`/`classList.add('dark')` 两行（index.html/login.html html 标签已于任务 2/3 去 dark，保名换值变量由 :root 浅色档供值，.dark 档仅作系统暗色兜底保留）；Chart.defaults.font.family 改 '"IBM Plex Mono", monospace'（与 tailwind.config.js mono 栈及 kiro2cc tailwind:114 一致），color/borderColor 两处 getThemeColor 经保名换值自动映射新令牌零改动 ②charts.js 15 处 getThemeColor 逐一核对：FAMILY_COLORS（neon-purple/green/cyan → brand/ok/brand）、tooltip（space-950 深色语义 + text-main/bright/dim + space-border）、MODEL_COLORS chart-1..16 均已指向新语义色值，霓虹 fallback（#a855f7/#22c55e/#06b6d4）在 CSS 变量恒有值前提下不可达，保留作防御性兜底 ③models.js thresholdColors 8 组硬编码霓虹 hex 全量换新令牌色（warn/mod-chart-14/brand/ok/danger/chart-12/13 顺序，shadow rgba 同步换值），getThresholdColor 取模消费逻辑零改动 ④model-dropdown.js focusBorderClass 三元（focus:!border-neon-purple/neon-cyan）改 'focus:border-brand' 单值（accentColor 参数保留兼容 5 个调用点，JSDoc 更新说明）；grep 全站 neon- 类名残留：views/index/login 均零残留 ⑤build:css 成功（559ms），产物核验：.focus\:border-brand:focus{border-color:var(--brand)} 收录、"IBM Plex Mono" 字体收录、border-neon 零残留 ⑥三文件 node 语法校验 OK

## 验收标准

- [x] 视觉还原：8 个页面（index + login + 6 views）全部呈现 kiro2cc admin-ui 风格 —— 浅色 surface 基调、IBM Plex 字体、232px 侧栏 + 点阵纹理、rounded-[11px] 卡片、四档渐变额度进度条、metrics 指标条、表格/按钮/输入框规格与 kiro2cc 一致（8/8 页 Playwright 截图 + DOM snapshot 核验通过，console 0 errors；唯一 warning 为 settings.html 既有 x-collapse 用法，非本次引入）
- [x] 功能零回归：全部 Alpine 绑定、i18n、modal、OAuth 添加账号流程、图表、日志流、设置读写功能与改造前一致（全部 Alpine 绑定逐字保留；js/components 仅 models.js thresholdColors 换值 + model-dropdown.js focusBorderClass 换令牌，消费逻辑零改动；保名换值令牌使 charts.js/app.js 零改动自动映射）
- [x] 构建可用：`npm run build:css` 构建成功且 public/css/style.css 产物完整；无 Tailwind purge 导致的样式缺失（最终构建 559ms，逐任务核验任意值类/任意变体全部收录）
- [x] 后端零改动：git diff 仅涉及 public/** 与 tailwind.config.js，src/ 与 tests/ 无变更
